# Architecture

Базовая структура проекта:

- `source/` — Windows-источник на Python;
- `server/` — центральный сервер;
- `client/` — браузерный клиент;
- `tests/` — тесты;
- `docs/` — документация.

Поток данных: захват экрана → H.264 → WebRTC/RTP/UDP → сервер → браузер.
